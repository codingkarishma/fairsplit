# FairSplit

Real-time bill splitting with OCR and live claiming.

## Receipt uploads

V1 supports JPG, JPEG, PNG, BMP, WebP, and TXT uploads up to 5MB. Image files
use Tesseract OCR; TXT files are parsed directly for testing. PDF uploads are
not supported in V1. Users can screenshot a PDF and upload the image instead.
